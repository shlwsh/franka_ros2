# 审稿人 A — 方法论视角

`[角色切换] 以下以 🔍 方法论审稿人身份独立审稿，不参考其他角色的结论。`

---

## 决定与评分

| 项 | 内容 |
|----|------|
| **决定** | Major Revision |
| **评分** | **7.0 / 10** |
| **信心** | 高（已核对 `table_ii.json`、`recommended_tau.json`） |

## 优点

1. **架构边界清晰**：IQA/路由置于 `franka_api_server` 网关，不侵入 1 kHz 控制回路（`03_system.tex`）。
2. **可复现协议**：500 帧 × 3 seeds、JSONL 日志、`run_matrix_tcm.py` + `recommended_tau.json` 管道明确（`05_1_setup.tex` L30–31）。
3. **诚实的 comparator 设计**：B3/B4 高 M2 与更高 RTT 分表报告，避免选择性隐瞒（`table_iii.tex` / `05_2_main_results.tex`）。
4. **τ 与 ROI 叙事一致**：主矩阵 τ_B2=0.465，full-frame 0.498 仅作消融（`05_1_setup.tex` L6–7）。

## Major Issues

### METHOD-M1: 主证据链仍为仿真延迟模型

- **位置**: `latex/sections/05_1_setup.tex` L28–29  
- **证据**: 云上传 uniform 50–550 ms；无现场医院 trace  
- **问题**: M1 绝对值不可外推至临床网络；相对排序虽合理，审稿人易质疑“208 ms”字面意义。  
- **建议**: 正文首段或图注加 **Simulated RTT** 标签；可选补充 `network_sweep_quick.json` 敏感性图入附录。  
- **验收标准**: Abstract/图 5 caption 含 “simulated” 或 “latency model”。  

### METHOD-M2: 标定分离度为负，需方法论层面解释

- **位置**: `experiments/results/recommended_tau.json` → `separation_min_clear_max_blur: -0.541`  
- **证据**: `median_clear` 0.516，`median_blur` 0.414，但 min-clear vs max-blur 分离为负  
- **问题**: 表明分数区间重叠；依赖中位数 τ 仍可运行，但审稿人会问 operating point 鲁棒性。  
- **建议**: 在 `05_4_tcm_validation.tex` 或主文用一小图/表报告 score 分布重叠率；与 B3 必要性挂钩。  
- **验收标准**: 正文显式引用该 JSON 字段或等价统计，非仅脚注。  

### METHOD-M3: 缺 NR-IQA / 外部路由基线（同协议）

- **位置**: `latex/sections/06_conclusion.tex` L13；`05_3_ablation.tex` 仅 B2 变体  
- **问题**: 消融未回答“是否 LangGraph 特有”，仅“τ/EMA/utility”变体。  
- **建议**: 至少 NIQE 或 BRISQUE + 同一 LangGraph 壳（附录表即可）；或明确列为 Future work 并在 Introduction 降调 B2 对比范围。  

### METHOD-M4: M2 为 scorer-internal，与临床质量脱钩

- **位置**: `latex/sections/05_5_discussion.tex` L36–37（已披露）  
- **问题**: 披露到位，但缺少与下游分类器置信度 even 小样本的相关性实验。  
- **建议**: 用 ShezhenV3 十类标签做 post-hoc：accepted vs rejected 帧的 classifier margin 分布（无需新采集）。  

## Minor Issues

- **METHOD-m1**: `05_2_main_results.tex` L31–32 内嵌 mini-Discussion，与 `05_5` 重复，可合并。  
- **METHOD-m2**: B4 定义在 `table_iii` 需一句算法说明（混合门控逻辑）便于复现。  

## 向作者提问

1. 物理 resampling（log-normal 50 ms）是否测过真实 `go_to_tongue_pose` skill 延迟分布？  
2. `separation_min_clear_max_blur < 0` 时，τ 敏感性曲线是否仍单峰？  

`[角色退出] 🔍 方法论审稿人 审稿完成`
