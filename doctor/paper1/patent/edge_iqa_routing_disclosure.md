# 专利交底提纲（可选）— Edge-IQA + 置信度路由

1. 一种边侧舌象采集质量评价方法，融合 Laplacian 清晰度与曝光适宜度得到 $Q_{img}$。  
2. 一种基于 $Q_{img}$ 与阈值 $\tau$、重试预算 $K$ 的三向路由方法（上传云 / 边缘重拍 / 失败安全）。  
3. 一种云–边–端协同采集闭环系统，边侧 FastAPI 网关通过子进程调用 IQA，LangGraph 兼容状态机记录 JSONL。  
4. 权利要求从属：$\tau$ 由验证集标定；重拍通过预定义 FR3 关节 Skills 执行。  

（正式文本待法务/导师审核）
