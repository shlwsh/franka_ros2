% §4.2 Confidence-Aware Routing — expanded with benchmark analysis
\subsection{Confidence-Aware Routing}
\label{sec:routing}

Given edge quality score $Q_{\mathrm{img}}$ and retry counter $r$, the router implements a testable three-way policy with threshold $\tau$ and budget $K$:
\begin{equation}
\text{route}(s)=
\begin{cases}
\text{upload\_cloud}, & Q_{\mathrm{img}}\geq\tau \land r\leq K,\\
\text{resample\_edge}, & Q_{\mathrm{img}}<\tau \land r\leq K,\\
\text{fail\_safe}, & r>K.
\end{cases}
\label{eq:routing}
\end{equation}

The retry budget $K$ limits the number of edge re-shoots before the system aborts; $K=2$ in our experiments.

\subsubsection{State machine nodes and edges.}
Table~\ref{tab:langgraph-nodes} enumerates the six nodes and their conditional edges implemented in \texttt{graph.py}. \texttt{TrialState} (a LangGraph-compatible TypedDict) carries \texttt{image\_path}, \texttt{q\_img}, \texttt{flags}, \texttt{retry\_count}, \texttt{route\_decision}, three ISO-8601 timestamps, and \texttt{latency\_ms}. All three branches and the $r>K$ guard are covered by unit tests (\texttt{tests/test\_routing.py}, 4 passing).

\begin{table}[t]
\centering
\caption{LangGraph nodes and conditional edges for closed-loop acquisition.}
\label{tab:langgraph-nodes}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lll}
\hline
Node & Input & Output / edge \\
\hline
\texttt{capture} & skill trigger & RGB frame path \\
\texttt{edge\_iqa} & frame path & $Q_{\mathrm{img}}$, \texttt{flags} \\
\texttt{route} & $Q_{\mathrm{img}}$, $r$ & decision \\
\texttt{resample\_edge} & decision & re-invoke \texttt{capture} \\
\texttt{upload\_cloud} & decision & cloud stub handoff \\
\texttt{fail\_safe} & $r>K$ & abort + JSONL log \\
\hline
\end{tabular}%
}
\end{table}

\begin{algorithm}[t]
\caption{Confidence-aware routing loop (B2)}
\begin{algorithmic}[1]
\STATE $r \leftarrow 0$, capture frame $I$
\REPEAT
  \STATE $(Q,\texttt{flags}) \leftarrow \text{EdgeIQA}(I)$
  \STATE $d \leftarrow \text{Route}(Q,r,\tau,K)$ \COMMENT{see Eq. \ref{eq:routing}}
  \IF{$d = \texttt{upload\_cloud}$}
    \STATE \textbf{break} \COMMENT{quality sufficient, cloud upload}
  \ELSIF{$d = \texttt{resample\_edge}$}
    \STATE $r \leftarrow r+1$; recapture $I$
  \ELSE
    \STATE \textbf{fail\_safe}; \textbf{break} \COMMENT{$r>K$, abort trial}
  \ENDIF
\UNTIL{$r > K$}
\end{algorithmic}
\label{alg:routing-loop}
\end{algorithm}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig2_routing_flow.pdf}
  \caption{LangGraph confidence-aware routing flow (nodes and conditional edges).}
  \label{fig:routing-flow}
\end{figure}

On ShezhenV3 replay, each \texttt{resample\_edge} applies a \emph{physical skill transform} before re-scoring: \texttt{adaptive\_visual\_damping} reduces Gaussian blur radius on the clear reference frame; \texttt{exposure\_loop\_tuning} applies gain/bias correction when exposure flags fire (\S\ref{sec:implementation}, \texttt{resample\_physics.yaml}).
No stochastic score inflation is applied.

\subsubsection{Why LangGraph over a plain Python FSM?}
The routing decision is fast enough that the choice of orchestration framework does not affect our RTT targets. We benchmarked six alternatives against LangGraph on 1\,000 trials (5 repeats, $\tau=0.505$, $K=2$):

\begin{itemize}
\item \textbf{Scenario A (routing-only):} pure \texttt{route()} call overhead (no I/O).
\item \textbf{Scenario B (full pipeline):} capture $\rightarrow$ Edge-IQA $\rightarrow$ route, including image processing.
\end{itemize}

Table~\ref{tab:fsm-benchmark} shows that routing overhead is below 5\,ms per 1\,000 trials for all frameworks, contributing \textbf{0.004\%} of the end-to-end RTT budget. In the full pipeline, I/O dominates 99.9\% of wall time; the LangGraph overhead over a plain FSM is 15\,\textmu s/trial (1.63\,\% of pipeline time), fully absorbed by the 10\,ms IQA step.

\begin{table}[t]
\centering
\caption{Routing framework benchmark: 1\,000 trials $\times$ 5 repeats.}
\label{tab:fsm-benchmark}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrrr}
\hline
Framework & Med. (ms) & P95 (ms) & LOC & Mem. (MB) \\
\hline
Plain Python & \textbf{1.32} & 2.74 & 15 & 34.2 \\
Vanilla FSM & 1.75 & 3.73 & 15 & 35.5 \\
Dataclass FSM & 7.32 & 10.09 & 26 & 37.6 \\
Pydantic FSM & 4.38 & 5.60 & 26 & 37.6 \\
Async FSM & 3.02 & 5.36 & 15 & 37.6 \\
\textbf{LangGraph (ours)} & 4.95 & 5.15 & 78 & 46.7 \\
\hline
\end{tabular}%
}
\vskip 2mm
\scriptsize
\raggedright
Scenario A (routing-only): LangGraph adds 3.63\,ms per 1\,000 trials ($\approx$3.6\,\textmu s/trial), 0.004\% of a 360\,ms RTT budget; full-pipeline I/O dominates ($>$99.9\%).
\end{table}

Beyond throughput, LangGraph provides three capabilities unavailable in plain Python FSMs: (1) \textbf{checkpoint persistence}---the graph can resume from any trial after interruption; (2) \textbf{conditional-edge visualization}---Fig. \ref{fig:routing-flow} is auto-generated from the graph definition; and (3) \textbf{reuse in Paper II}, where multi-agent orchestration requires the same graph abstraction.

\subsubsection{Algorithm extensions (B2d/B2m/B2u).}
Baseline B2 uses fixed $\tau_{B2}$ from validation medians. Three extensions (implemented in \texttt{dynamic\_tau.py}, \texttt{multi\_frame\_iqa.py}, \texttt{utility\_route.py}) raise algorithmic novelty without changing the primary deployable path:
\begin{align}
\tau_t &= \tau_0 + \alpha \tfrac{\mathrm{RTT}_t}{\mathrm{RTT}_{\mathrm{ref}}} + \beta P_{\mathrm{loss}} \label{eq:dynamic-tau}\\
Q_t &= \lambda Q_{t-1} + (1-\lambda)\, Q_{\mathrm{img}} \label{eq:ema-iqa}\\
U &= w_1 Q - w_2 \tfrac{\mathrm{RTT}}{\mathrm{RTT}_{\mathrm{ref}}} - w_3 C \label{eq:utility}
\end{align}
\textbf{B2d} adapts $\tau_t$ under network stress (higher RTT or loss $\Rightarrow$ stricter upload gate).
\textbf{B2m} fuses a short burst of micro-captures via EMA before routing.
\textbf{B2u} replaces Eq.~\ref{eq:routing} with $U\geq u^\ast \Rightarrow \texttt{upload\_cloud}$.
Quick ablation (60 frames, seed~0, physical resample): B2 M2$=$0.60; B2m/B2u M2$=$0.65 with modest RTT overhead for B2m (+69\,ms p50 vs.\ B2); full 3-seed numbers pending \texttt{paper1\_run\_tcm\_full.sh}.

\subsection{Closed-Loop Protocol and Logging}
\label{sec:closed-loop}

Each trial executes \texttt{capture} $\rightarrow$ \texttt{edge\_iqa} $\rightarrow$ \texttt{route} $\rightarrow$ action. Wall-clock segment timestamps and the decision are appended as one JSON object per line (JSONL) for supplementary audit trails and auxiliary M6 trend analysis.

Required JSONL fields: \texttt{trial\_id}, \texttt{image\_path} (relative, privacy-safe), \texttt{q\_img}, \texttt{flags}, \texttt{retry\_count}, \texttt{route\_decision}, \texttt{t\_capture}, \texttt{t\_iqa}, \texttt{t\_route}, \texttt{latency\_ms}. Automated validation: \texttt{scripts/validate\_jsonl.py}.

The driver \texttt{python\ -m\ langgraph\_router.run\ --trials\ 10} produces \texttt{experiments/logs/run\_001.jsonl}. Main quantitative RTT (M1) is computed offline by \texttt{run\_matrix.py} on ShezhenV3 frames; JSONL logs serve protocol demonstration and trend verification only.

JSONL stores relative paths; supplementary samples use synthetic or de-identified frames. No raw patient data appears in Paper~I outputs.
