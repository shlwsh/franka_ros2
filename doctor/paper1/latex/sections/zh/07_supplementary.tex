补充材料随仓库提供：\texttt{run\_001.sample.jsonl}（脱敏 trial 日志）、\texttt{REPRODUCE.md}（离线矩阵）及 \texttt{REPRODUCE\_franka.md}（$\geq$10 次网关闭环）。
图~S1 汇总辅轨 M6 RTT 直方图（\texttt{fig\_s1\_franka\_rtt.pdf}）。
不含可识别患者的原始图像。

\begin{figure}[h]
  \centering
  \includegraphics[width=0.85\linewidth]{fig_s1_franka_rtt.pdf}
  \caption{辅轨闭环时延（M6，$n{=}50$）。}
  \label{fig:s1-franka-zh}
\end{figure}
