% 补充材料（中文扩展）
\paragraph{一键复现.}
仓库根目录执行 \texttt{bash scripts/paper1\_run\_tcm\_full.sh}：导入划分、训练 B3、ROI 标定 $\tau$、主矩阵（3 seeds）、图表与 PDF。

\paragraph{工件清单.}
\begin{itemize}
  \item \texttt{tcm\_\{train,val,test\}.json} --- 6719 张，含 COCO \texttt{bboxes}；
  \item \texttt{recommended\_tau.json} --- ROI 标定（$n{=}1144$）；
  \item \texttt{roi\_ablation.json} --- 全图 vs 舌区 ROI；
  \item \texttt{b3\_mobilenet.pt}、\texttt{b3\_training\_meta.json} --- B3 权重与验证准确率；
  \item \texttt{calibration\_val.csv}、\texttt{main\_seed*\_detail.csv} --- 分数与帧级日志。
\end{itemize}

\paragraph{环境.}
Edge-IQA 仅需 NumPy/Pillow；B3 训练使用 \texttt{doctor/paper1/.venv}（PyTorch）。
WSL 下数据集路径见 \texttt{tcm\_paths.yaml}。

\paragraph{超参数.}
Edge-IQA：512 缩放，0.65/0.35 权重；ROI 8\% padding。
B3：MobileNetV3-Small，224 输入，AdamW $10^{-3}$，5 epoch，batch 64。
路由：$\tau$ 来自验证集中位数，$K{=}2$。

\paragraph{JSONL 十字段.}
\texttt{trial\_id}、\texttt{image\_path}、\texttt{q\_img}、\texttt{flags}、\texttt{retry\_count}、\texttt{route\_decision}、\texttt{latency\_ms}、\texttt{t\_capture}、\texttt{t\_iqa}、\texttt{t\_route}。

\paragraph{局限.}
合成高斯模糊；运行时需框或检测器；B3 与 Edge-IQA 的 $\tau$ 标度不一致；M6 不与表 II 直接对比。

\paragraph{伦理.}
使用公开 ShezhenV3-COCO；JSONL 不含患者标识。

\paragraph{未来工作.}
端侧舌检测 ROI；混合 Edge-IQA+B3 头；自 FR3 日志采集运动模糊 augment。

\paragraph{表 S1：ROI 消融（摘要）.}
全图 $\tau{=}0.498$，ROI $\tau{=}0.465$；详见 \texttt{roi\_ablation.json}。

\paragraph{表 S2：B3 训练（摘要）.}
5594 训练图，11188 对，验证准确率 98.3\%。

\paragraph{Listing S2：标定命令.}
\texttt{python3 experiments/edge_iqa/calibrate\_tau\_tcm.py}

\paragraph{Listing S3：B3 训练命令.}
\texttt{.venv/bin/python experiments/edge\_iqa/train\_b3\_mobilenet.py}

\paragraph{Listing S4：主矩阵.}
\texttt{.venv/bin/python experiments/run\_matrix\_tcm.py}

\paragraph{版本记录.}
2026-05-30：ROI + 真实 B3 + ShezhenV3 全量验证纳入正文。

\paragraph{联系维护.}
算法问题见 \texttt{docs-zh/paper1/study/} 学习手册六章。

\paragraph{许可.}
代码 Apache-2.0；ShezhenV3 遵循其公开许可。

\paragraph{图 S1.}
M6 Franka RTT 直方图见 \texttt{fig\_s1\_franka\_rtt.pdf}。

\paragraph{扩展阅读.}
V19 大纲 \texttt{docs-zh/paper1/V19/}；网关 ENV \texttt{docs-zh/paper1/V19/ENV.md}。

\paragraph{故障排查.}
若 \texttt{b3\_mobilenet.pt} 缺失，主矩阵 B3 回退 \texttt{b3\_q\_boost}（已默认关闭 \texttt{b3\_use\_learned}）。

\paragraph{性能提示.}
B3 训练在 CPU 上约 45 分钟（5594 图 $\times$ 5 epoch）；可减小 \texttt{--max-train} 做 smoke test。

\paragraph{中文稿说明.}
本中文稿与英文稿章节同步；表 II 由 \texttt{sync\_table\_ii\_zh.py} 自动生成。

\paragraph{致谢占位.}
相机就绪版补充基金与临床合作单位。

\paragraph{数据不包含项.}
投稿包不含原始患者图像，仅含统计与脚本。

\paragraph{安全提示.}
生产环境务必配置 \texttt{FRANKA\_API\_KEY}。

\paragraph{Closing.}
补充材料旨在使审稿人与学生可在无 FR3 硬件条件下复现 Table II 与标定数字。
