% §5.2 主实验结果 — ShezhenV3 全量验证
\subsection{主实验结果}
\label{sec:main-results-zh}

\input{sections/zh/table_ii}

表~\ref{tab:main-results-zh} 汇总主基线 B0--B2（物理 Skill 重采，$\tau_{B2}$，3 seeds）。
表~\ref{tab:learned-hybrid-zh} 报告 MobileNet B3（$\tau_{B2}$ / $\tau_{B3}$）与混合门控 B4。

\input{sections/zh/table_iii}

相对 B0，B2 在物理重采模型下提升 M2；B3 在 $\tau_{B2}$ 下路由 M2 偏低，$\tau_{B3}$ 重标定与 B4 混合策略见表~\ref{tab:learned-hybrid-zh}。

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{fig5_rtt_cdf.pdf}
  \caption{端到端 RTT CDF（ShezhenV3 测试集，物理重采，3 seeds）。}
  \label{fig:rtt-cdf-zh}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.65\linewidth]{fig6_valid_rate.pdf}
  \caption{有效帧率 M2；误差棒为 seeds 间标准差。}
  \label{fig:valid-rate-zh}
\end{figure*}

\paragraph{讨论。}
绝对 RTT 取决于延迟模型；相对顺序反映物理重采减少无效云往返。

\textbf{结论句。} B2 仍为首选部署路径；B4 混合 Tier-A/B 为学习型 IQA 的完整协作流水线。
