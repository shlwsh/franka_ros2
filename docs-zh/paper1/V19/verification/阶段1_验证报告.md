# 阶段 1 验证报告 — 系统基座与架构证据

> **验证时间（UTC）**：2026-05-29T08:47:08Z  
> **验证方式**：`scripts/verify_paper1_phases.sh`（自动，无需手工）  
> **仓库**：`/root/work/franka_ros2`  
> **PAPER1_ROOT**：`/root/work/franka_ros2/doctor/paper1`

---

## 1. 验证结论

| 项目 | 结果 | 说明 |
|------|------|------|
| 环境检查 `check_paper1_env.sh` | **通过** | 15/15 项（API 常驻未起为预期警告） |
| `doctor/paper1` 目录骨架 | **通过** | edge_iqa、langgraph_router、franka_bridge、latex、figures |
| `PAPER1_MODE=1` 禁用 controller | **通过** | `GET /controller/list` → **HTTP 404** |
| Skills 列表 API | **通过** | 返回 `go_to_tongue_pose`, `go_to_face_pose` |
| Skills 执行 API | **接受** | HTTP 200；无 MoveIt 时 `status=failed`（无 Action Server，属环境限制） |
| Vision 端点存在 | **通过** | 阶段 2 已升级为 edge_iqa；阶段 1 契约已满足 |
| `franka_api_server` pytest | **通过** | 13/13（含 skills、vision、paper1_mode） |
| Fig.1 素材 | **通过** | `figures/fig1_system_overview.svg` |
| §3 LaTeX 草稿 | **通过** | `latex/sections/03_system.tex` |

**阶段 1 总评：通过**

---

## 2. 自动化证据摘录

### 2.1 环境检查

```
通过: 15  失败: 0
```

### 2.2 Skills API（uvicorn 冒烟，`PAPER1_MODE=1`）

```json
{"skills":["go_to_tongue_pose","go_to_face_pose"]}
```

```json
{"task_id":"skill_e88a270b","status":"failed","message":"skill=go_to_tongue_pose: No Action Server (MoveIt, Trajectory or PTP) available"}
```

说明：路由与 yaml 加载正常；完整运动需 `scripts/testall.sh` 启动 MoveIt。

### 2.3 Controller 禁用

```
controller/list HTTP 404
```

### 2.4 单元测试

```
13 passed in 12.51s
```

---

## 3. 交付物核对

| 路径 | 状态 |
|------|------|
| `franka_api_server/franka_api_server/config.py` | 含 `paper1_mode`, `paper1_root` |
| `franka_api_server/franka_api_server/skills/poses.yaml` | 存在 |
| `franka_api_server/franka_api_server/routers/vision.py` | 存在 |
| `franka_api_server/franka_api_server/routers/motion.py` | skills 端点 |
| `doctor/paper1/figures/fig1_system_overview.svg` | 存在 |
| `doctor/paper1/latex/sections/03_system.tex` | 存在 |
| `doctor/paper1/sim/franka_bridge/client.py` | 存在 |

---

## 4. 映射任务 ID

| 任务 | 状态 |
|------|------|
| F0-01～F0-03 | 通过 |
| F1-01～F1-04 | 通过 |
| P1-T01、P1-T02、P1-T04 | 通过 |

---

## 5. 复现

```bash
cd /root/work/franka_ros2
bash scripts/verify_paper1_phases.sh
# 或分项：
bash scripts/check_paper1_env.sh
export PAPER1_MODE=1
# 启动 testall 后：
curl -s "http://127.0.0.1:8000/api/v1/motion/skills?api_key=franka-api-default-key"
```

原始日志：[logs/run_20260529T084708Z.log](./logs/run_20260529T084708Z.log)
