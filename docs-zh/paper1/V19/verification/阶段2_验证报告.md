# 阶段 2 验证报告 — Edge-IQA 与阈值标定

> **验证时间（UTC）**：2026-05-29T08:47:08Z  
> **验证方式**：`scripts/verify_paper1_phases.sh` + 离线 benchmark  
> **PAPER1_ROOT**：`/root/work/franka_ros2/doctor/paper1`

---

## 1. 验证结论

| 项目 | 指标 / 期望 | 实测 | 结果 |
|------|-------------|------|------|
| 清晰 vs 模糊区分 | 均值差 > 0.2 | 见 §2.1 | **通过** |
| 模糊样本 `blur` 标志 | 含 `blur` flag | 通过 | **通过** |
| edge_iqa 单测 | 3 passed | 3 passed | **通过** |
| 离线延迟 p95 @512px | < 30 ms | **9.57 ms** | **通过** |
| API vision 实装 | scorer=edge_iqa | 是 | **通过** |
| 推荐阈值 τ | 文档化 | **0.505** | **通过** |
| Fig.3 | PDF 存在 | `fig_iqa_hist.pdf` | **通过** |
| §4.1 LaTeX | 存在 | `04_1_edge_iqa.tex` | **通过** |

**阶段 2 总评：通过**

---

## 2. 量化结果

### 2.1 判别力（合成 val，抽检 5 对）

自动脚本统计（与 pytest `test_clear_vs_blur_separation` 一致）：

|  cohort | q_img 范围（5 张抽检） |
|--------|----------------------|
| clear | 约 0.79–0.83 |
| blur | 约 0.16–0.22 |
| **分离度** | min(clear) − max(blur) **> 0.55** |

全量标定（240 张）：

| 指标 | 值 |
|------|-----|
| median_clear | 0.819 |
| median_blur | 0.191 |
| **推荐 τ** | **0.505** |

来源：`experiments/results/recommended_tau.json`

### 2.2 离线延迟（`latency_benchmark.json`）

```json
{
  "n": 100,
  "resize": 512,
  "p50_ms": 8.009,
  "p95_ms": 9.567,
  "pass_p95_under_30ms": true
}
```

### 2.3 CLI 抽样

```json
{"q_img": 0.8075, "flags": [], "t_iqa_ms": 15.511, "meta": {"scorer": "edge_iqa", "version": "0.2.0"}}
```

图像：`experiments/debug/sample_clear.png`

### 2.4 API `POST /vision/evaluate`（子进程挂载）

```json
{
  "q_img": 0.8075,
  "flags": [],
  "t_iqa_ms": 26.878,
  "threshold_tau": 0.505,
  "meta": {"scorer": "edge_iqa", "version": "0.2.0", "mode": "subprocess"}
}
```

`threshold_tau` 与 `recommended_tau.json` 一致。  
HTTP 路径含网关与子进程开销，**t_iqa_ms ≈ 27 ms** 仍远小于 35 ms 辅轨门槛。

---

## 3. 交付物核对

| 路径 | 状态 |
|------|------|
| `edge_iqa/scorer.py`, `cli.py` | 存在 |
| `edge_iqa/tests/test_scorer.py` | 3 passed |
| `experiments/results/calibration_val.csv` | 240 行 |
| `experiments/edge_iqa/latency_benchmark.json` | p95 通过 |
| `figures/fig_iqa_hist.pdf` / `.svg` | 存在 |
| `latex/sections/04_1_edge_iqa.tex` | 存在 |
| `franka_api_server/.../services/paper1_iqa.py` | 子进程桥接 |
| `scripts/paper1_iqa_bench.sh` | 存在（需 API 常驻） |

---

## 4. 映射任务 ID

| 任务 | 状态 |
|------|------|
| P2-T01～P2-T04 | 通过 |
| F2-01～F2-04 | 通过 |

---

## 5. 数据说明

当前 val 为 **`scripts/synth_degrade.py` 合成数据**（120 clear + 120 blur），用于无 TCM 全量时的阶段闸门。接入真实 TCM-Tongue 后仅需重跑：

```bash
python3 experiments/edge_iqa/calibrate_tau.py
python3 figures/plot_iqa_hist.py
```

接口与 API **不变**。

---

## 6. 复现

```bash
cd /root/work/franka_ros2/doctor/paper1
python3 -m pytest edge_iqa/tests -q
python3 experiments/edge_iqa/latency_benchmark.py
python3 -m edge_iqa.cli --image experiments/debug/sample_clear.png --json
```

原始日志：[logs/latency_20260529T084708Z.json](./logs/latency_20260529T084708Z.json)、[logs/vision_eval_20260529T084708Z.json](./logs/vision_eval_20260529T084708Z.json)
